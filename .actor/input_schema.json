{
  "title": "LinkedIn Post Scraper Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "profileUrl": {
      "title": "Profile URL or Username",
      "type": "string",
      "description": "LinkedIn profile URL (e.g. https://www.linkedin.com/in/satyanadella) or just the username",
      "editor": "textfield",
      "example": "https://www.linkedin.com/in/satyanadella"
    },
    "postUrl": {
      "title": "Single Post URL",
      "type": "string",
      "description": "Single LinkedIn post URL to fetch",
      "editor": "textfield"
    },
    "postUrls": {
      "title": "Post URLs (Batch)",
      "type": "array",
      "editor": "stringList",
      "items": { "type": "string" },
      "description": "Array of LinkedIn post URLs to fetch in batch"
    },
    "count": {
      "title": "Number of Posts",
      "type": "integer",
      "description": "Number of posts to fetch from a profile",
      "default": 10,
      "minimum": 1,
      "maximum": 100
    },
    "apiKey": {
      "title": "API Key",
      "type": "string",
      "description": "Backend API key (optional, uses default if not provided)",
      "editor": "textfield",
      "isSecret": true
    }
  },
  "required": []
}
